#file: noinspection YAMLSchemaValidation
name: Create PR for dev
on:
  push:
    branches:
      - dev

env:
  GH_TOKEN: ${{ github.token }}

jobs:
  prepare-versions-dev:
    outputs:
      pnpm_version: ${{ steps.pnpm-version.outputs.prop }}
      node_version: ${{ steps.node-version.outputs.prop }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Retrieve Node Version
        id: node-version
        uses: notiz-dev/github-action-json-property@v0.2.0
        with:
          path: 'package.json'
          prop_path: 'engines.node'
      - name: Retrieve PNPM Version
        id: pnpm-version
        uses: notiz-dev/github-action-json-property@v0.2.0
        with:
          path: 'package.json'
          prop_path: 'engines.pnpm'
  build-and-test-dev:
    needs: prepare-versions-dev
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v3
        with:
          node-version: ${{ needs.prepare-versions-dev.outputs.node_version }}
      - run: git config user.name "github-action[bot]"
      - run: git config user.email "<>"
      - run: npm install -g pnpm@${{ needs.prepare-versions-dev.outputs.pnpm_version }}
      - run: pnpm install
      - run: pnpm build
      - run: pnpm test
      - run: gh pr close release/dev | exit 0
      - run: gh pr create --base release/dev --fill --title "dev -> release/dev" | exit 0
